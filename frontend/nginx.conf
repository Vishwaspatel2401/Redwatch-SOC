server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Required for React Router — all routes must return index.html
    # so the client-side router handles /app/dashboard, /login, etc.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
